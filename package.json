{
  "name": "ccs-main-site",
  "version": "1.0.0",
  "description": "Cargo Care Solutions Main site",
  "main": "canvas-animation.js",
  "scripts": {
    "build": "npm run minify-js",
    "minify-js": "uglifyjs canvas-animation.js -o canvas-animation.min.js && uglifyjs deep-dive.js -o deep-dive.min.js && uglifyjs script.js -o script.min.js && uglifyjs video.js -o video.min.js && uglifyjs lashing-animation.js -o lashing-animation.min.js",
    "deploy": "wrangler publish",
    "dev": "wrangler dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "uglify-js": "^3.17.4",
    "wrangler": "^3.28.2"
  }
}
